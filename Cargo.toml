[package]
name = "corefs"
version = "0.1.12"
edition = "2024"

[dependencies]
bincode = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_os = "linux")'.dependencies]
fuser = "0.14"
libc = "0.2"
