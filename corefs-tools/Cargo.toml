[package]
name = "corefs-tools"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
description = "CoreFS administrative tools — mkfs, fsck, scrub, dump, defrag, snapshot, … as programmatic APIs with structured reports."

[dependencies]
corefs-core = { path = "../corefs-core", features = ["std"] }
corefs = { path = ".." }
serde = { workspace = true, features = ["std"] }
serde_json = "1"

[dev-dependencies]
bincode = { workspace = true }
